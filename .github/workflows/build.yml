name: Upload to Appetize.io (Demo)

on:
  push:
    branches: [ main ]

jobs:
  upload:
    runs-on: ubuntu-latest

    steps:
    - name: Download test APK
      run: |
        wget https://github.com/lineageos4microg/Signal-Android/raw/lineage-21/app/build/outputs/apk/release/app-universal-release-unsigned.apk -O app.apk

    - name: Upload to Appetize.io
      run: |
        curl -H "Authorization: Bearer tok_o3laoux75kpoudqemidnbxfvpq" \
             -F "file=@app.apk" \
             -F "platform=android" \
             https://api.appetize.io/v1/apps
